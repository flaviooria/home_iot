[project]
name = "subscriber"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "shared",
]

[tool.uv.sources]
shared = { workspace = true, editable = true }
