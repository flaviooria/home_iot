[project]
name = "subscriber"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiomqtt>=2.5.1",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true, editable = true }
