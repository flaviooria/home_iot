[project]
name = "shared"
version = "0.1.0"
description = "Library to generate tables and models from IoT devices"
readme = "README.md"
authors = [
    { name = "Flavio Oria", email = "flavio.oria@bosonit.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
