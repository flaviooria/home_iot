[project]
name = "home-iot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.18.4",
]

[tool.uv.workspace]
members = [
    "api",
    "subscriber",
    "shared",
    "publisher",
]


